================================================================================
ИСПРАВЛЕНИЯ ДЛЯ РАЗДЕЛА "ЗАВИСИМОСТЬ ОТ ДЛИНЫ ТРУБОК"
================================================================================

⚠️ ПРОБЛЕМА:
В текущем тексте написано про "экспоненциальную зависимость σ = σ₀·exp(α·L)",
но на графике используется НЕПРАВИЛЬНЫЙ тренд!

✅ РЕШЕНИЕ:
На полулогарифмическом графике (log₁₀(σ) vs L) для экспоненциальной
зависимости нужен ЛИНЕЙНЫЙ тренд, а не экспоненциальный!

================================================================================
ЧТО ИЗМЕНИТЬ В EXCEL:
================================================================================

1. УДАЛИ текущий тренд (экспоненциальный y = 1.4068e^0.0019x)

2. ДОБАВЬ ЛИНЕЙНЫЙ ТРЕНД:
   - Правая кнопка на точках → "Добавить линию тренда"
   - Тип: ЛИНЕЙНАЯ
   - Опции: ✓ Показать уравнение, ✓ Показать R²
   
3. ПОЛУЧИШЬ уравнение вида:
   y = B·x + A
   
   где:
   - y = log₁₀(σ) 
   - x = L
   - B = наклон прямой
   - A = свободный член

4. ФИЗИЧЕСКАЯ ИНТЕРПРЕТАЦИЯ:
   log₁₀(σ) = A + B·L
   
   Откуда:
   σ = 10^A · 10^(B·L)
   σ = σ₀ · exp(α·L)
   
   где:
   - σ₀ = 10^A
   - α = B · ln(10) ≈ B · 2.303

================================================================================
ИСПРАВЛЕННЫЙ ТЕКСТ (ПАРАГРАФЫ 22-25):
================================================================================

СТАРАЯ ВЕРСИЯ (УДАЛИТЬ):
────────────────────────
"В диапазоне L = 200-500 нм проводимость подчиняется экспоненциальной 
зависимости:

σ = σ₀ · exp(α·L)

где α = 0.0019 нм⁻¹. Аппроксимация даёт σ₀ = 1.41 См/м, что соответствует 
базовому уровню проводимости вблизи порога перколяции. За исследуемый 
диапазон длин проводимость возрастает более чем в 100 раз (от σ ≈ 46 См/м 
при L = 200 нм до σ ≈ 16400 См/м при L = 500 нм)."


НОВАЯ ВЕРСИЯ (ВСТАВИТЬ):
────────────────────────
"В диапазоне L = 200-500 нм проводимость подчиняется экспоненциальной 
зависимости σ = σ₀ · exp(α·L). На полулогарифмическом графике (log₁₀(σ) vs L) 
эта зависимость проявляется как линейная:

log₁₀(σ) = [A] + [B]·L

где коэффициенты определены методом наименьших квадратов (R² = [значение из Excel]).
Из наклона прямой следует показатель экспоненты α = [B]·ln(10) ≈ [B·2.303] нм⁻¹, 
а базовая проводимость σ₀ = 10^[A] ≈ [значение] См/м.

За исследуемый диапазон длин проводимость возрастает более чем в 100 раз 
(от σ ≈ 46 См/м при L = 200 нм до σ ≈ 16400 См/м при L = 500 нм), что 
соответствует увеличению экспоненты на Δ(α·L) ≈ 4.7.

Экспоненциальный характер зависимости объясняется увеличением среднего 
числа контактов на трубку: более длинные трубки пересекают большее число 
соседних элементов, что приводит к резкому снижению эффективного 
сопротивления сетки. Относительно большие планки погрешностей при L = 200 нм 
(CV ≈ 195%) связаны с близостью к порогу перколяции, где система 
чувствительна к топологии конкретной реализации."


📝 ИНСТРУКЦИЯ:
────────────────
1. Построй ЛИНЕЙНЫЙ тренд в Excel на графике log₁₀(σ) vs L
2. Запиши значения A, B и R² из уравнения тренда
3. Вычисли: α = B × 2.303 и σ₀ = 10^A
4. Подставь значения в текст выше (заменив [значения] на реальные числа)
5. Скопируй исправленный текст в Word документ

================================================================================
ДРУГИЕ РАЗДЕЛЫ — ПРОВЕРКА
================================================================================

✅ Анализ зависимости σ(φ) — ВСЁ ПРАВИЛЬНО
   - S-образная кривая
   - φ_c = 0.27%
   - Степенной закон t = 1.86
   → НЕ ТРЕБУЕТ ИСПРАВЛЕНИЙ

✅ Влияние ориентационного разброса (jitter) — ВСЁ ПРАВИЛЬНО
   - Критическая роль контактов
   - Оптимум при 15-30°
   → НЕ ТРЕБУЕТ ИСПРАВЛЕНИЙ

================================================================================
ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ
================================================================================

Для полноты анализа можешь ДОБАВИТЬ (опционально):

1. В конце раздела про σ(L):
   
   "Следует отметить, что экспоненциальный рост справедлив при условии
   φ > φ_c. При L < 200 нм система находится вблизи порога перколяции,
   где эффект длины нелинейен и определяется дискретным характером
   формирования проводящей сетки."

2. Общее заключение по геометрии:
   
   "Таким образом, проводимость композита критически зависит от
   геометрических параметров УНТ: экспоненциально от длины (×100 при
   увеличении L в 2.5 раза) и немонотонно от ориентационного разброса
   с оптимумом при jitter ≈ 15-30°. Идеальное выравнивание (jitter = 0°)
   приводит к полной потере перколяции несмотря на φ >> φ_c."

================================================================================
